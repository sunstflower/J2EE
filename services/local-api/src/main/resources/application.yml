spring:
  application:
    name: local-api
  datasource:
    url: jdbc:sqlite:./.runtime/local-api/local-api.db
    driver-class-name: org.sqlite.JDBC

app:
  session:
    token: ${APP_SESSION_TOKEN:dev-session-token}

server:
  port: 0
  address: 127.0.0.1
